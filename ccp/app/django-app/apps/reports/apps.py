"""Stub AppConfig for :mod:`apps.reports`. Replaced by Unit 10."""

from django.apps import AppConfig


class ReportsConfig(AppConfig):
    """Placeholder config for the HTML reports app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.reports"
    label = "reports"
