"""Stub AppConfig for :mod:`apps.core`. Replaced by Units 2/3/4/12."""

from django.apps import AppConfig


class CoreConfig(AppConfig):
    """Placeholder config for the shared core services app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.core"
    label = "core"
