"""Stub AppConfig for :mod:`apps.integrations`. Replaced by Unit 11."""

from django.apps import AppConfig


class IntegrationsConfig(AppConfig):
    """Placeholder config for the optional integrations app (AI, PI, Sentry)."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.integrations"
    label = "integrations"
