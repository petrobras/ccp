"""Stub AppConfig for :mod:`apps.straight_through`. Replaced by Unit 5."""

from django.apps import AppConfig


class StraightThroughConfig(AppConfig):
    """Placeholder config for the straight-through compressor page."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.straight_through"
    label = "straight_through"
