"""Stub AppConfig for :mod:`apps.curves_conversion`. Replaced by Unit 7."""

from django.apps import AppConfig


class CurvesConversionConfig(AppConfig):
    """Placeholder config for the curves-conversion page."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.curves_conversion"
    label = "curves_conversion"
